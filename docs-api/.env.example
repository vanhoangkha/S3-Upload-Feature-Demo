# Environment variables for docs-api
# Copy this file to .env and update the values as needed
# Values below are based on Terraform outputs from docs-infra

# AWS Configuration
AWS_REGION=ap-northeast-3

# Note: AWS credentials are automatically detected from:
# 1. AWS CLI profile (~/.aws/credentials)
# 2. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY) - optional
# 3. EC2/ECS/Lambda roles when deployed

# DynamoDB Tables (from Terraform outputs)
DOCUMENTS_TABLE_NAME=docs-management-dev-documents

# S3 Buckets (from Terraform outputs)
DOCUMENT_STORE_BUCKET_NAME=docs-management-dev-document-store-2026-by-ctn
WEB_STORE_BUCKET_NAME=docs-management-dev-web-store-2026-by-ctn

# Cognito Configuration (from Terraform outputs)
# Get actual values by running: terraform output cognito_user_pool_id
COGNITO_USER_POOL_ID=ap-northeast-3_xxxxxxxxx

# API Gateway (from Terraform outputs)
# Get actual value by running: terraform output api_gateway_url
API_GATEWAY_URL=https://xxxxxxxxxx.execute-api.ap-northeast-3.amazonaws.com/dev

# Local Development
PORT=3001
NODE_ENV=development

# CORS allowed origins (from Terraform configuration)
ALLOWED_ORIGINS=http://localhost:3000

# Presigned URL expiry (in seconds)
PRESIGNED_URL_EXPIRY=3600

# ===========================================
# To get actual values after infrastructure deployment:
# ===========================================
# cd docs-infra
# terraform output api_env_config
